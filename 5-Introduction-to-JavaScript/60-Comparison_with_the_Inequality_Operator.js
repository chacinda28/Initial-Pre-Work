// Setup
function testNotEqual(val) {
  if (val != 99) { // Change this line  //Use the operator Inequality (!=).
    return "Not Equal";
  }
  return "Equal";
}

// Change this value to test
testNotEqual(10);
