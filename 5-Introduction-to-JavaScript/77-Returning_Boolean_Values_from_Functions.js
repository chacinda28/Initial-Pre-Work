function isLess(a, b) {
  // Fix this code
  return (a < b);
  }

// Change these values to test
isLess(15,10);

//This is the better code (using the function isless) to return true o false.
