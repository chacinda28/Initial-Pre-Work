// Setup
function compareEquality(a, b) {
  if (a === b) { // Change this line // Use the operator strict with comparing different values.
    return "Equal";
  }
  return "Not Equal";
}

// Change this value to test
compareEquality(10, "10");
