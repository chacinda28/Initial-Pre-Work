// Setup
function testEqual(val) {
  if (val == 12 ) { // Change this line   //I am learning the use the operator equal with the conditional if.
    return "Equal";
  }
  return "Not Equal";
}

// Change this value to test
testEqual(10);
